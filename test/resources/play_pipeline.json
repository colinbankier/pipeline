{ "name": "Pipeline",
  "tasks": [
    { "name": "Init message", "command": "echo starting" },
    { "name": "Git Clone", "command": "pwd; if [ -d pipeline ]; then cd pipeline && git fetch && git checkout origin/master; else hub clone colinbankier/pipeline; fi;" },
    { "name": "task 2",
      "tasks": [
        { "name": "task 2a", "command": "cd pipeline; mix deps.get && mix compile" },
        { "name": "task 2b", "command": "cd pipeline; mix test" },
        { "name": "task 2c", "command": "echo 2c" }
      ]
    },
    { "name": "task 3", "command": "" }
  ]
}
